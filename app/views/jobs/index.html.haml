- if !user_signed_in?
	.jumbotron
		%h1 Need a job?
		%p Find the bests jobs in one click!
		%p #{link_to "Sign Up", new_user_registration_path, class: "btn btn-primary"} or #{link_to "Sign In", new_user_session_path, class: "btn btn-success"}


- @jobs.each do |job|
	.col-md-4
		%h1= link_to job.title, job_path(job)
		%p= job.category.name
		%p= job.description
		%hr
